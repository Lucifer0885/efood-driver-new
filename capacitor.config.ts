import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efood.driver.app',
  appName: 'efood-driver',
  webDir: 'dist/efood-driver/browser',
};

export default config;
